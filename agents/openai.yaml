interface:
  display_name: "originplot"
  short_description: "Build editable Origin figures with scaled verification."
  default_prompt: "Use $originplot to build an editable Origin OPJU with the appropriate quick, standard, or release profile, then reopen it and verify native Worksheet bindings."

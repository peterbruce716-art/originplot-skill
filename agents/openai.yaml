interface:
  display_name: "originplot"
  short_description: "Discover templates and build verified, editable Origin OPJU figures."
  default_prompt: "Use $originplot to find official template candidates or plan and build a source-traceable editable Origin OPJU, then reopen it, verify native Worksheet bindings, export from Origin, and report source-fidelity and publication QA separately."
